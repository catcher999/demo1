## 这是当前版本
我在设计3的基础上做了一些改动，反映管理端 3 批次落地、算力流水账与操作审计的加入、限流改 IP 维度、敏感信息外部化等。
我继续放弃关注线程，异常，内存泄漏问题。

### 需要注意的
在controller中不直接做逻辑判断（复用 Request DTO 或拆解为基本类型），而是调用 service实现，
使用common.Result 包装DTO，返回结果给前端
DTO只在前后端交互（Controller 接收/返回）使用
内部层之间（Service↔Mapper）直接传递 Entity 即可
    ：Controller ↔ Service：极简原则：除非 Service 需要接收 5 个以上参数，否则不需要专门定义"内部 DTO"，直接传基本类型或 Entity。
    ：Service ↔ Mapper：直接使用 Entity

#### Mapper 使用原则
简单 CRUD（单表查询）用 wrapper，不需要 XML。
复杂查询（多表 JOIN、动态条件）用 XML。
两者可以在同一个 Mapper 接口里共存。

#### VO / DTO / Entity 区分
Entity：对应数据库表，Service ↔ Mapper 之间传递用。
VO（View Object）：返回给前端的视图对象，Service 组装后给 Controller，隐藏不需要暴露的字段。
Request DTO：前端发过来的请求参数对象（如 LoginRequest）。
有状态的（DTO、VO、Entity）不加 Spring 注解，每次 new。
无状态的（Service、Mapper、Util）加 Spring 注解，由容器注入。

#### 配置文件分工
application.properties：提交到 git，只放非敏感配置（host、port、url、连接参数等）。
application-local.properties：被 .gitignore 忽略，本机敏感配置（密码、密钥、授权码、API Key）。
通过 spring.config.import=optional:application-local.properties 引入，local 文件不存在不报错。

#### 敏感信息硬编码禁令（新增）
- 邮箱账号/授权码、API Key、支付私钥、数据库密码、JWT 密钥、Redis 密码 全部存 application-local.properties
- Service 实现类禁止硬编码邮箱/密钥，统一通过 @Value 注入
- .gitignore 必须包含 application-local.properties

#### 审计与流水账原则（新增）
- 算力变动（充值/任务扣减/退还/签到/管理员调整）必须先写 points_log 流水，再改 user.points（同一事务内）
- 管理端高危操作（算力调整/启禁用用户/上下架套餐/启禁用模型/手动补单）必须同步写 operation_log 审计表
- 普通查询操作不打审计（避免吞吐压力）
- 审计表永久存档，便于事故追溯

#### 不怎么重要的笔记
什么时候需要"专门内部 DTO"？

跨服务调用（微服务）：A服务 调 B服务 的 API，需要 DTO 作为序列化载体。

多表 JOIN 结果映射：ArtworkMapper 连表查询查出 Artwork + Category 的部分字段，此时 Mapper 可以返回 ArtworkVO，
但这属于 Mapper 的 ResultMap 映射，与前端显示层的 VO 是两码事。
----

### 目录结构

> 包组织原则：**先分层，层内按模块分子包**。
> 一个包里若只有一个类，则不再单独拆子包（如 controller 层、consumer 层、task 层保持平铺）。
> service 层每个模块子包内含接口 + impl 子包（接口和实现同属模块，方便对照）。

- **src/main/java/com.example.demo/**
  - **controller** — 【接口层/表现层】（10 个类，平铺不分模块）
    - AuthController.java — 登录/登出/发验证码
    - UserController.java — 个人信息（profile）、修改密码、每日签到领算力
    - GalleryController.java — 画廊列表（公开）、作品详情、点赞/取消点赞（需登录）
    - TaskController.java — AI 生成任务的会话/提交/确认/取消/发布/查询
    - RechargeController.java — 充值套餐、创建订单、查询订单、支付宝回调
    - AdminUserController.java — 管理端：用户列表/详情/修改/启禁用/调整算力（5 接口）
    - AdminPackageController.java — 管理端：套餐列表/添加/修改/上下架（4 接口）
    - AdminModelController.java — 管理端：模型列表/添加/修改（含算力单价）/启禁用（4 接口）
    - AdminTaskController.java — 管理端：任务流水列表/详情（2 接口）
    - AdminOrderController.java — 管理端：订单列表/详情/手动补单（3 接口）
    - > 注：禁止出现 SQL 或复杂 if-else，职责仅：接参数 -> 调 Service -> 返回 Result JSON。
  - **service** — 【业务逻辑层】按模块分子包，每个模块含接口 + impl 子包
    - **auth** — AuthService / impl.AuthServiceImpl：登录、验证码、登出、自动注册
    - **user** — UserService / impl.UserServiceImpl：个人信息、修改密码
    - **points** — PointsService / impl.PointsServiceImpl：算力扣减/退还/签到奖励/管理员调整（Lua 原子防超扣 + 写流水）
    - **gallery** — ArtworkService / impl.ArtworkServiceImpl：作品列表、详情、点赞（Redis 防刷 + 热度计算）
    - **task** — TaskService、AiSessionService、LlmService、ImageGenerationService / impl.* ：任务编排、会话、LLM、图片生成（策略模式可插拔）
    - **recharge** — RechargeService、AlipayService / impl.RechargeServiceImpl、impl.AlipayServiceImpl：订单、支付链接、回调验签、掉单补偿
    - **mail** — MailService / impl.MailServiceImpl：验证码邮件、任务结果通知
    - **admin** — AdminUserService、AdminPackageService、AdminModelService、AdminTaskService、AdminOrderService / impl.* ：管理端运营管控（含审计写入）
    - > 注：Controller 依赖 Service 接口，不依赖实现类。
  - **consumer** — 【MQ 消费者】（2 个类，平铺）
    - TaskConsumer.java — 消费 task.queue，生成图片、更新状态、发邮件、失败退算力（手动 ACK）
    - OrderCloseConsumer.java — 消费 order.close.delay.queue，自动关单（延迟队列）
  - **task** — 【定时任务】（1 个类，平铺）
    - OrderCompensateTask.java — 掉单补偿，定时扫描未回调订单主动查询支付宝
  - **mapper** — 【持久层】按模块分子包
    - **user** — UserMapper
    - **gallery** — ArtworkMapper（+XML：selectPublicArtworks/incrementLike/decrementLike/incrementView）、CategoryMapper
    - **task** — TaskMapper、AiSessionMapper、AiModelMapper
    - **recharge** — RechargeOrderMapper、RechargePackageMapper
    - **admin** — PointsLogMapper、OperationLogMapper
    - > 注：简单 CRUD 用 wrapper，复杂 JOIN/动态条件用 XML，可在同一 Mapper 共存。
  - **entity** — 【实体类】按模块分子包
    - **user** — User（含 points、status、signDate、signStreak、role）
    - **gallery** — Artwork（含 likes、views、heatScore）、Category
    - **task** — Task（含 pointsCost、model、queuedAt、finishedAt）、AiSession、AiModel
    - **recharge** — RechargeOrder（含 tradeNo、updatedAt）、RechargePackage（含 type、createdAt、updatedAt）
    - **admin** — PointsLog（算力流水账）、OperationLog（操作审计）
  - **dto** — 【数据传输对象】按模块分子包
    - **auth** — LoginRequest、EmailLoginRequest、SendCodeRequest、LoginResponse
    - **user** — UpdateProfileRequest、UpdatePasswordRequest、UserVO
    - **gallery** — ArtworkVO
    - **task** — CreateTaskRequest、PublishTaskRequest、UpdatePreferenceRequest、TaskVO、SessionVO、TaskMessage（MQ 消息体）
    - **recharge** — CreateOrderRequest、PaymentResponse、RechargeOrderVO、RechargePackageVO
    - **admin** — UserAdminVO、UpdateUserRequest、UpdateUserStatusRequest、AdjustPointsRequest、CreatePackageRequest、UpdatePackageRequest、PackageAdminVO、CreateModelRequest、UpdateModelRequest、ModelAdminVO、TaskAdminVO、OrderAdminVO
  - **config** — 【配置类】
    - MybatisPlusConfig.java — 分页插件
    - RedisConfig.java — RedisTemplate 序列化
    - WebMvcConfig.java — 拦截器注册（JwtInterceptor + AdminInterceptor）
    - LlmConfig.java — LLM 配置 + 带超时 RestTemplate
    - RabbitMQConfig.java — task.exchange/queue/dlx + order.close 延迟队列
    - AlipayConfig.java — 支付宝沙箱配置
  - **common** — 【公共工具/异常/拦截器】
    - Result.java、ResultCode.java、BusinessException.java、GlobalExceptionHandler.java
    - JwtInterceptor.java（注入 currentUserId + currentRole）、JwtUtil.java
    - AdminInterceptor.java（校验 role=admin，拦截 /api/admin/**）

- **src/main/resources/**
  - application.properties — 公开配置（提交 git）
  - application-local.properties — 敏感配置（被 .gitignore 忽略）
  - **mapper**
    - ArtworkMapper.xml — selectPublicArtworks（JOIN category）+ incrementLike/decrementLike/incrementView（热度计算 likes×5+views×1）

### 数据库表结构

#### user 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| name | VARCHAR(50) | 用户名（验证码登录后留空，待用户设置） |
| password | VARCHAR(100) | 密码（BCrypt 加密，验证码登录的账号为 null） |
| email | VARCHAR(100) | 邮箱（索引 idx_email） |
| role | VARCHAR(20) | 角色，默认 'user'，管理员为 'admin' |
| points | INT | 算力点数余额（默认0，签到/充值增加，任务消耗） |
| status | TINYINT | 账号状态（1=正常，0=禁用） |
| sign_date | DATE | 最近一次签到日期（防重复签到） |
| sign_streak | INT | 连续签到天数（计算阶梯奖励） |

#### category 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| name | VARCHAR(50) | 分类名称 |
| description | VARCHAR(200) | 分类描述 |
| sort_order | INT | 排序序号 |

#### artwork 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| title | VARCHAR(100) | 作品标题 |
| description | VARCHAR(500) | 作品描述 |
| image_url | VARCHAR(255) | 图片URL |
| artist | VARCHAR(50) | 作者 |
| date | DATETIME | 创作时间 |
| heat_score | INT | 综合热度分（= likes×5 + views×1，数据库原子更新） |
| likes | INT | 点赞数（默认0） |
| views | INT | 浏览数（默认0） |
| category_id | BIGINT | 关联 category.id |
| is_public | TINYINT(1) | 是否公开（默认1） |
| user_id | BIGINT | 创建者ID |

#### ai_session 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 创建者ID |
| title | VARCHAR(100) | 会话标题（由首个任务的 AI 返回自动生成） |
| preference | TEXT | 用户风格偏好（对抗长上下文遗忘） |
| created_at | DATETIME | 创建时间（数据库默认值） |
| updated_at | DATETIME | 更新时间（数据库 ON UPDATE 自动更新） |

#### ai_task 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| session_id | BIGINT | 所属会话ID（索引） |
| user_id | BIGINT | 创建者ID（索引） |
| prompt | VARCHAR(500) | 用户输入的提示词 |
| description | TEXT | AI 生成的结构化描述 |
| status | VARCHAR(20) | 任务状态（pending/waiting_confirm/queued/running/succeeded/failed/cancelled） |
| result_url | VARCHAR(500) | 生成图片URL |
| error_message | VARCHAR(500) | 失败原因 |
| artwork_id | BIGINT | 关联 artwork.id（发布后回填） |
| points_cost | INT | 本次任务消耗的算力（确认时记录，失败/取消时退还） |
| model | VARCHAR(50) | 使用的模型标识（如 mock-image） |
| queued_at | DATETIME | 进入队列时间 |
| finished_at | DATETIME | 完成时间（成功/失败/取消） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### ai_model 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| name | VARCHAR(50) | 模型标识（如 mock-image） |
| display_name | VARCHAR(100) | 展示名称 |
| points_cost | INT | 单次调用消耗算力 |
| status | TINYINT | 启用状态（1=启用，0=禁用） |

#### recharge_package 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| name | VARCHAR(50) | 套餐名称 |
| points | INT | 套餐算力点数 |
| price | DECIMAL(10,2) | 价格（元） |
| type | VARCHAR(20) | 套餐类型（once 一次性 / monthly 月卡） |
| status | TINYINT | 上架状态（1=上架，0=下架；添加不等于上架） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### recharge_order 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| order_no | VARCHAR(64) | 商户订单号（唯一） |
| user_id | BIGINT | 下单用户ID |
| package_id | BIGINT | 套餐ID |
| package_name | VARCHAR(50) | 套餐名称（冗余字段，避免套餐改名影响历史订单） |
| points | INT | 套餐算力点数 |
| amount | DECIMAL(10,2) | 实付金额 |
| pay_method | VARCHAR(20) | 支付方式（alipay / wechat，第一版只支持 alipay） |
| trade_no | VARCHAR(64) | 支付宝交易号（回调后回填） |
| status | VARCHAR(20) | 订单状态（pending/paid/closed/refunded） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### points_log 表（新增 · 算力流水账）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| user_id | BIGINT | 用户ID（索引） |
| delta | INT | 变动值（正=增加，负=扣减） |
| balance_after | INT | 变动后余额 |
| source | VARCHAR(20) | 来源：recharge/task_cost/refund/sign/admin_adj |
| operator_id | BIGINT | 操作人ID（admin_adj 时记录管理员，其他来源为 NULL） |
| remark | VARCHAR(200) | 备注 |
| created_at | DATETIME | 创建时间（索引） |

#### operation_log 表（新增 · 操作审计）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键，自增 |
| operator_id | BIGINT | 操作人ID（管理员，索引） |
| operation | VARCHAR(50) | 操作类型，如 ADJUST_POINTS/DISABLE_USER/CREATE_PACKAGE/LIST_PACKAGE/COMPENSATE_ORDER |
| target_type | VARCHAR(20) | 目标类型：USER/POINTS/PACKAGE/MODEL/ORDER |
| target_id | BIGINT | 目标ID（联合索引 idx_target） |
| request_params | VARCHAR(500) | 请求参数JSON |
| ip | VARCHAR(50) | 操作IP |
| status | TINYINT | 1=成功 0=失败 |
| error_msg | VARCHAR(500) | 失败原因 |
| created_at | DATETIME | 操作时间（索引） |

### API 接口

#### 用户端 · 认证
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| POST | /api/auth/login | 邮箱+密码登录，返回 token | {email, password} |
| POST | /api/auth/send-code | 发送验证码到邮箱（5分钟有效，60秒防刷，每日5次） | {email} |
| POST | /api/auth/email-login | 验证码登录，用户不存在则自动注册 | {email, code} |
| POST | /api/auth/logout | 登出 | 无 |

#### 用户端 · 画廊
| 方法 | 路径 | 说明 | 登录 |
|------|------|------|------|
| GET | /api/gallery/artworks | 分页查询公开作品 | 否 |
| GET | /api/gallery/artworks/{id} | 作品详情（浏览数+1，含点赞状态） | 是 |
| POST | /api/gallery/artworks/{id}/like | 点赞（Redis 防重复） | 是 |
| POST | /api/gallery/artworks/{id}/unlike | 取消点赞 | 是 |

参数：page（默认1）、size（默认10）、categoryId（可选）、sortBy（默认heatScore，可选date，白名单防注入）

#### 用户端 · 用户（全部需登录）
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| GET | /api/user/profile | 获取个人信息 | 无 |
| PUT | /api/user/profile | 修改用户名 | {name} |
| PUT | /api/user/password | 修改密码（首次设置时 oldPassword 传 null） | {oldPassword?, newPassword} |
| POST | /api/user/sign | 每日签到领算力（防重复签到，连续签到阶梯奖励） | 无 |

#### 用户端 · 充值（除回调外需登录）
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| GET | /api/recharge/packages | 套餐列表（仅上架的） | 无 |
| POST | /api/recharge/orders | 创建订单，返回支付宝支付链接 | {packageId} |
| GET | /api/recharge/orders | 我的订单列表（分页） | page, size |
| GET | /api/recharge/orders/{orderNo} | 查询订单详情（校验归属权） | 无 |
| POST | /api/recharge/notify | 支付宝异步回调（验签 + 加算力，无需登录） | 支付宝表单 |
| GET | /api/recharge/return | 支付宝同步跳转（无需登录） | 无 |

#### 用户端 · AI 生成任务（全部需登录）
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| POST | /api/tasks/sessions | 创建会话 | 无 |
| GET | /api/tasks/sessions | 我的会话列表 | page, size |
| PUT | /api/tasks/sessions/{id}/preference | 更新风格偏好 | {preference} |
| POST | /api/tasks | 提交生成请求（并发控制：有 waiting_confirm 则拒绝；按 IP 10秒防刷） | {sessionId, prompt} |
| POST | /api/tasks/{id}/confirm | 确认描述，扣算力并推 MQ 异步生成 | 无 |
| POST | /api/tasks/{id}/cancel | 取消任务（waiting_confirm/queued 可取消，queued 退算力） | 无 |
| POST | /api/tasks/{id}/publish | 发布到画廊 | {title, categoryId?, isPublic?} |
| GET | /api/tasks/{id} | 查任务详情 | 无 |
| GET | /api/tasks | 任务列表（可按 session 过滤） | sessionId?, page, size |

#### 管理端 · 鉴权（新增）
- AdminInterceptor 拦截 `/api/admin/**`，校验 JWT 中 role=admin
- 复用用户端登录接口 `/api/auth/login`，管理员账号通过 user.role=admin 标识
- JwtInterceptor 注入 currentRole 到 request attribute，AdminInterceptor 二次校验

#### 管理端 · 用户管理（新增 · 5 接口）
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| GET | /api/admin/users | 用户列表（分页+模糊搜索+状态过滤） | page, size, keyword?, status? |
| GET | /api/admin/users/{id} | 用户详情 | 无 |
| PUT | /api/admin/users/{id} | 修改用户信息（合并字段） | {name?, email?, ...} |
| PUT | /api/admin/users/{id}/status | 启用/禁用用户 | {status} |
| POST | /api/admin/users/{id}/points | 调整算力（delta 可正负，写流水+审计） | {delta, remark} |

#### 管理端 · 套餐管理（新增 · 4 接口）
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| GET | /api/admin/packages | 套餐列表（含下架的） | page, size, status? |
| POST | /api/admin/packages | 添加套餐（status 默认 0=下架，添加≠上架） | {name, points, price, type?} |
| PUT | /api/admin/packages/{id} | 修改套餐（按需传字段） | {name?, points?, price?, type?} |
| PUT | /api/admin/packages/{id}/status | 上架/下架 | {status} |

#### 管理端 · 模型管理（新增 · 4 接口）
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| GET | /api/admin/models | 模型列表 | page, size, status? |
| POST | /api/admin/models | 添加模型（status 默认 0=禁用） | {name, displayName, pointsCost} |
| PUT | /api/admin/models/{id} | 修改模型（含算力单价 pointsCost 调整） | {name?, displayName?, pointsCost?} |
| PUT | /api/admin/models/{id}/status | 启用/禁用模型 | {status} |

#### 管理端 · 任务流水（新增 · 2 接口）
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| GET | /api/admin/tasks | 全站任务流水（按状态/用户过滤） | page, size, status?, userId? |
| GET | /api/admin/tasks/{id} | 任务详情 | 无 |

#### 管理端 · 订单管理（新增 · 3 接口）
| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| GET | /api/admin/orders | 订单列表（按状态/订单号/用户过滤） | page, size, status?, orderNo?, userId? |
| GET | /api/admin/orders/{orderNo} | 订单详情 | 无 |
| POST | /api/admin/orders/{orderNo}/compensate | 手动补单（复用掉单补偿逻辑，加审计） | 无 |

### Task 模块状态机

```
pending → waiting_confirm → queued → running → succeeded
    ↓           ↓              ↓         ↓
  failed    cancelled      cancelled   failed
```

- pending：任务刚创建，未调 AI
- waiting_confirm：AI 已返回文字描述，等用户确认
- queued：用户确认，已扣算力，消息已推入 MQ 待消费（可取消，退算力）
- running：消费者正在生成图片
- succeeded：图片生成成功
- failed：任一阶段失败（消费者失败会自动退算力）
- cancelled：用户取消（waiting_confirm / queued 均可取消，queued 退算力）

并发控制：同会话下有 waiting_confirm 状态的任务时，拒绝新的提交请求。
防刷：任务提交按 IP 限流，同一 IP 10 秒内只能提交一次（Redis key: task_limit:ip:{ip}）。
异步化：confirmTask 先扣算力再推 MQ，立即返回 queued；TaskConsumer 消费时改 running，成功 succeeded，失败 failed + 退算力 + NACK 进死信队列。

### Task 模块架构设计

- LLM 服务和图片生成服务均采用策略模式（@ConditionalOnProperty 控制），可插拔切换
- 第一版 LLM 用 DeepSeek（OpenAI 兼容接口，30s 超时），图片生成用 Mock（占位图）
- 上下文历史：调 LLM 时，同会话下 succeeded 的任务按时间排序拼成 messages
- 标题生成：对齐 DeepSeek 网页版做法，AI 返回时同时生成标题，首个任务后回填到 session
- 发布逻辑：task 成功后用户手动发布到画廊，不自动写入 artwork 表

### 鉴权规则

#### 用户端
JwtInterceptor 拦截 /api/**，排除以下路径（无需登录可访问）：
- /api/auth/login
- /api/auth/send-code
- /api/auth/email-login
- /api/gallery/artworks（仅画廊列表公开，详情/点赞需登录）
- /api/recharge/notify（支付宝异步回调）
- /api/recharge/return（支付宝同步跳转）

其他所有 /api/** 路径需在 Header 携带 `Authorization: Bearer {token}`。
JwtInterceptor 在 request attribute 注入 `currentUserId` 和 `currentRole`。

#### 管理端
AdminInterceptor 拦截 `/api/admin/**`，校验 `currentRole == "admin"`，否则抛 BusinessException。
用户端与管理端共用同一套 JWT 体系，role 字段决定访问权限。

### 算力与支付

#### 算力变动闭环（新增流水账）
- 所有算力变动（充值/任务扣减/退还/签到/管理员调整）必须先写 points_log 流水，再改 user.points
- points_log.balance_after 字段记录变动后余额，可重放还原任意时刻余额
- 对账任务：定时 SUM(delta) == 当前余额，不一致告警（待实现）

#### 算力操作
- 算力扣减：PointsService 用 Lua 脚本保证原子性，余额不足返回 false
- 算力退还：任务失败/queued 取消时，按 points_cost 原子退还，并写流水
- 签到奖励：每日一次（sign_date 防重），连续签到阶梯加成（sign_streak），并写流水
- 管理员调整：adminAdjustPoints 方法，delta 可正负，事务内写流水 + 改余额 + 写审计

#### 支付流程
- 创建订单(pending) → 返回支付链接 → 用户支付 → 异步回调验签 → 改 paid + 加算力 + 写流水
- 自动关单：订单创建后推延迟队列，30 分钟超时未支付自动改 closed（RabbitMQ 延迟队列实现）
- 掉单补偿：OrderCompensateTask 定时扫描 pending 订单，主动查询支付宝补回调
- 手动补单：管理员通过 POST /api/admin/orders/{orderNo}/compensate 触发，复用掉单补偿逻辑，加审计

### 管理端审计规则（新增）

| 操作类型 | 是否写流水 | 是否写审计 | 理由 |
|---------|----------|----------|------|
| 算力变动（充值/任务/签到/管理员调整） | ✅ 必须 | ✅ 必须 | 资金属性，无追溯=设计缺陷 |
| 用户状态变更（启用/禁用） | ❌ 不需要 | ✅ 必须 | 影响用户登录，必须可追溯 |
| 用户信息变更（改名/改邮箱） | ❌ 不需要 | ⚠️ 只 log.info | 非核心，DB 审计过度 |
| 套餐上下架 | ❌ 不需要 | ✅ 必须 | 影响用户充值可选范围 |
| 模型启禁用 | ❌ 不需要 | ✅ 必须 | 影响用户任务可选模型 |
| 算力单价调整 | ❌ 不需要 | ✅ 必须 | 影响用户任务成本 |
| 手动补单 | ✅ 必须 | ✅ 必须 | 资金属性 + 管理员操作 |
| 查询类（列表/详情） | ❌ 不需要 | ❌ 不需要 | 读操作 |

### 外部服务
- Redis：Redis Cloud 云服务（Lettuce 客户端，Spring Boot 默认）。用途：验证码（verify_code:{email}，TTL 5分钟+随机抖动）、防重复点赞（like:{userId}:{artworkId}，30天TTL）、任务防刷（task_limit:ip:{ip}，同一 IP 10秒）、算力原子操作（Lua 脚本）
- Mail：QQ 邮箱 SMTP（端口 587，STARTTLS），用于发送验证码邮件 + 任务结果通知；账号/授权码存 application-local.properties，通过 @Value 注入
- LLM：DeepSeek API（OpenAI 兼容接口，30s 超时，api-key 在 application-local.properties），策略模式可插拔（llm.provider 切换）
- 图片生成：Mock 占位图（picsum.photos），策略模式可插拔，后续可换 Pollinations/SD
- RabbitMQ：本地 Docker 容器，手动 ACK。task.exchange + task.dlx（任务异步生成 + 死信）、order.close.delay（订单延迟关单）
- 支付宝沙箱：RSA2 验签，私钥在 application-local.properties，notify/return 回调
- API 文档：springdoc-openapi，启动后访问 /swagger-ui.html

### 待办

#### 已完成
- [x] WebMvcConfig：注册 JwtInterceptor + AdminInterceptor
- [x] GlobalExceptionHandler：全局异常处理
- [x] 密码加密：BCryptPasswordEncoder
- [x] LoginRequest 填充字段
- [x] JWT 密钥加强：32 字节 Base64
- [x] ResultCode 填充枚举
- [x] Task 模块实现（含会话/任务/LLM/图片生成/发布）
- [x] 用户模块（个人信息、修改密码、每日签到领算力）
- [x] 算力服务（Lua 原子扣减/退还/签到奖励）
- [x] Task 异步化改造（queued 状态 + RabbitMQ + 消费者手动 ACK + 失败退算力）
- [x] 画廊点赞接口（Redis 防重复 + 热度计算 likes×5+views×1）
- [x] 支付宝沙箱支付模块（订单/支付链接/回调验签/延迟关单/掉单补偿）
- [x] 包结构重组（分层 + 层内按模块分子包）
- [x] 任务提交限流改 IP 维度（task_limit:ip:{ip}）
- [x] 敏感信息外部化（邮箱/密钥/API Key 全部入 application-local.properties）
- [x] 管理端 批次1：鉴权 + 用户管理（AdminInterceptor + 5 接口 + points_log + operation_log）
- [x] 管理端 批次2：套餐 + 模型管理（8 接口 + 审计写入）
- [x] 管理端 批次3：任务流水 + 订单管理（5 接口 + 手动补单复用补偿逻辑）
- [x] 设计4.md 文档同步

#### 待办
- [ ] 联调测试：应用启动 + 接口连通性验证
- [ ] SSE 实时通知：替代前端轮询，提升用户体验（当前已有 GET /api/tasks/{id} 轮询接口）
- [ ] 算力对账任务：定时 SUM(delta) == user.points 校验
- [ ] 文件上传接口（项目硬要求未列）
- [ ] 搜索/分类管理（项目硬要求未列）
